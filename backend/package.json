{
  "name": "infinitydeck-backend",
  "version": "0.0.0",
  "description": "Backend for InfinityDeck-app.",
  "main": "app.js",
  "scripts": {
    "start": "node ./dist/app.js",
    "dev": "npm run build && node --env-file=.env ./dist/app.js",
    "format": "prettier --write src/ --config .prettierrc.json --cache",
    "build": "npx tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.3.0",
    "ws": "^8.14.2"
  },
  "type": "commonjs",
  "devDependencies": {
    "prettier": "^3.0.3",
    "@types/ws": "^8.5.10",
    "typescript": "^5.3.3"
  }
}